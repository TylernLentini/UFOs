let friends = ["Sarah", "Greg", "Cindy", "Jeff"];

// create loop to iterate through an array in JS
function listLoop(userList) {
    for (var i = 0; i < userList.length; i++) {
      console.log(userList[i]);
    }
 }

 // call the function
 listLoop(friends);

 //returns each name as an object